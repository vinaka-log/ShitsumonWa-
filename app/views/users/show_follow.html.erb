<div class = "container follow-page text-center">
  <div class = "row">
    <div class="col-md-offset-4 col-md-4">
      <% @user.followings.each do |user| %>
        <table>
          <tr>
            <td>  <%= user.name %> </td>
            <td><%= link_to 'Detail', user_path(user) %></td>
          </tr> 
        </table>
      <% end %>
    </div>
  </div>
</div>